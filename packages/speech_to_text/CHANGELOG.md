## 0.1.1

- Make permission requests asynchronous without blocking the platform thread.
- Prevent queued native callbacks from accessing a disposed plugin.

## 0.1.0

- Initial Tizen implementation for `speech_to_text`.
